query SearchEntries($filter: EntryFilterInput) {
    entries(
        first: 10
        where: $filter
    ) {
        nodes {
            id
            name
            summary
            entryType
            categories {
                id
                name
                icon
            }
        }
    }
}
