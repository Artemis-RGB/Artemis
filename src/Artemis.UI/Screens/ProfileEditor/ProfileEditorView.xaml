<UserControl x:Class="Artemis.UI.Screens.ProfileEditor.ProfileEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:s="https://github.com/canton7/Stylet"
             xmlns:profileEditor="clr-namespace:Artemis.UI.Screens.ProfileEditor"
             xmlns:behaviors="clr-namespace:Artemis.UI.Behaviors"
             xmlns:dataTemplateSelectors="clr-namespace:Artemis.UI.DataTemplateSelectors"
             xmlns:profile="clr-namespace:Artemis.Core;assembly=Artemis.Core"
             xmlns:shared="clr-namespace:Artemis.UI.Shared;assembly=Artemis.UI.Shared"
             mc:Ignorable="d"
             behaviors:InputBindingBehavior.PropagateInputBindingsToWindow="True"
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=profileEditor:ProfileEditorViewModel, IsDesignTimeCreatable=False}">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary
                    Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.TextBlock.xaml" />
                <ResourceDictionary
                    Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.PopupBox.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <UserControl.InputBindings>
        <KeyBinding Command="{s:Action Undo}" Modifiers="Control" Key="Z" />
        <KeyBinding Command="{s:Action Redo}" Modifiers="Control" Key="Y" />
    </UserControl.InputBindings>

    <Grid ClipToBounds="True">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <shared:ProfileConfigurationIcon Grid.Row="0"
                                         Grid.Column="0"
                                         Width="25"
                                         Height="25"
                                         Margin="25 -5 25 0"
                                         ToolTip="{Binding ProfileConfiguration.Name}"
                                         ConfigurationIcon="{Binding ProfileConfiguration.Icon}" />

        <Menu Grid.Row="0" Grid.Column="1" IsMainMenu="True" Margin="0 -4 0 0">
            <MenuItem Header="_File">
                <MenuItem Header="New"  Icon="{materialDesign:PackIcon Kind=Plus}">
                    <MenuItem Header="Category" Icon="{materialDesign:PackIcon Kind=FormatListBulletedType}" />
                    <MenuItem Header="Profile" Icon="{materialDesign:PackIcon Kind=BookEdit}" />
                    <MenuItem Header="Folder" Icon="{materialDesign:PackIcon Kind=Folder}" />
                    <MenuItem Header="Layer" Icon="{materialDesign:PackIcon Kind=Layers}" />
                </MenuItem>
                <MenuItem Header="Export..." Icon="{materialDesign:PackIcon Kind=Export}" />
                <MenuItem Header="Import..." Icon="{materialDesign:PackIcon Kind=Import}" />
            </MenuItem>
            <MenuItem Header="_Edit">
                <MenuItem Header="_Duplicate" Command="Cut" Icon="{materialDesign:PackIcon Kind=ContentDuplicate}" InputGestureText="Ctrl+D" />
                <MenuItem Header="_Copy" Command="Copy" Icon="{materialDesign:PackIcon Kind=ContentCopy}" InputGestureText="Ctrl+C" />
                <MenuItem Header="_Paste" Command="Paste" Icon="{materialDesign:PackIcon Kind=ContentPaste}" InputGestureText="Ctrl+V" />
            </MenuItem>
            <MenuItem Header="_Scripting">
                <MenuItem Header="_Profile scripts" Icon="{materialDesign:PackIcon Kind=BookEdit}" />
                <MenuItem Header="_Layer scripts" Icon="{materialDesign:PackIcon Kind=Layers}" />
                <MenuItem Header="_Property scripts" Icon="{materialDesign:PackIcon Kind=FormTextbox}" />
            </MenuItem>
            <MenuItem Header="_Help">
                <MenuItem Header="Artemis FAQ" Icon="{materialDesign:PackIcon Kind=BookEdit}" />
                <Separator />
                <MenuItem Header="Editor" Icon="{materialDesign:PackIcon Kind=Edit}" />
                <MenuItem Header="Layers" Icon="{materialDesign:PackIcon Kind=Layers}" />
                <MenuItem Header="Display conditions" Icon="{materialDesign:PackIcon Kind=NotEqual}" />
                <MenuItem Header="Data bindings" Icon="{materialDesign:PackIcon Kind=VectorLink}" />
                <MenuItem Header="Scripting" Icon="{materialDesign:PackIcon Kind=CodeJson}" />
                <Separator />
                <MenuItem Header="Report a bug" Icon="{materialDesign:PackIcon Kind=Github}" />
                <MenuItem Header="Get help on Discord" Icon="{materialDesign:PackIcon Kind=Discord}" />
            </MenuItem>
        </Menu>

        <Grid Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Margin="10 -5 10 10">
            <Grid.ColumnDefinitions>
                <!-- Left side -->
                <ColumnDefinition Width="*" MinWidth="100" />
                <!-- Side panels resize -->
                <ColumnDefinition Width="Auto" />
                <!-- Side panels -->
                <ColumnDefinition Width="{Binding SidePanelsWidth.Value, Mode=TwoWay}" MinWidth="100" />
            </Grid.ColumnDefinitions>

            <!-- Left side -->
            <Grid Grid.Row="0" Grid.Column="0">
                <Grid.RowDefinitions>
                    <!-- Design area -->
                    <RowDefinition Height="*" MinHeight="200" />
                    <!-- Bottom panels resize -->
                    <RowDefinition Height="Auto" />
                    <!-- Bottom panels -->
                    <RowDefinition Height="{Binding BottomPanelsHeight.Value, Mode=TwoWay}" MinHeight="108" />
                </Grid.RowDefinitions>

                <!-- Design area -->
                <materialDesign:Card Grid.Row="0" materialDesign:ShadowAssist.ShadowDepth="Depth1" VerticalAlignment="Stretch">
                    <ContentControl s:View.Model="{Binding ProfileViewModel, IsAsync=True}" />
                </materialDesign:Card>

                <!-- Bottom panels resize -->
                <GridSplitter Grid.Row="1" Grid.Column="0" Height="5" HorizontalAlignment="Stretch" Cursor="SizeNS" Margin="0 5" />

                <!-- Bottom panels -->
                <Grid Grid.Row="2">
                    <!-- Layer elements -->
                    <materialDesign:Card Grid.Column="0" materialDesign:ShadowAssist.ShadowDepth="Depth1" VerticalAlignment="Stretch">
                        <ContentControl s:View.Model="{Binding LayerPropertiesViewModel, IsAsync=True}" />
                    </materialDesign:Card>
                </Grid>
            </Grid>

            <!-- Side panels resize -->
            <GridSplitter Grid.Row="0" Grid.Column="1" Width="5" HorizontalAlignment="Stretch" Cursor="SizeWE" Margin="5 0" />

            <!-- Side panels -->
            <Grid Grid.Row="0" Grid.Column="2">
                <Grid.RowDefinitions>
                    <!-- Profile elements -->
                    <RowDefinition Height="*" MinHeight="100" />
                    <!-- Conditions resize -->
                    <RowDefinition Height="Auto" />
                    <!-- Display conditions -->
                    <RowDefinition Height="{Binding DataModelConditionsHeight.Value, Mode=TwoWay}" MinHeight="100" />
                </Grid.RowDefinitions>

                <!-- Profile elements -->
                <materialDesign:Card Grid.Row="0" materialDesign:ShadowAssist.ShadowDepth="Depth1" VerticalAlignment="Stretch">
                    <ContentControl s:View.Model="{Binding ProfileTreeViewModel, IsAsync=True}" Margin="0,-1,-0.2,1" />
                </materialDesign:Card>

                <!-- Conditions resize -->
                <GridSplitter Grid.Row="1" Height="5" HorizontalAlignment="Stretch" Cursor="SizeNS" Margin="0 5" />

                <!-- Display conditions -->
                <materialDesign:Card Grid.Row="2" materialDesign:ShadowAssist.ShadowDepth="Depth1" VerticalAlignment="Stretch">
                    <ContentControl s:View.Model="{Binding DisplayConditionsViewModel, IsAsync=True}" />
                </materialDesign:Card>
            </Grid>
        </Grid>
    </Grid>


</UserControl>